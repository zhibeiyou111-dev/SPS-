# SPS Reproduction Code

This directory contains the runnable code for the SPS CIFAR-100-LT IR=100 reproduction bundle.

For the paper summary, selected tables, dataset layout, and checkpoint information, see:

```text
../README_PACKAGE.md
```
